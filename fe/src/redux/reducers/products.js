const initialState = {
  globalState1: "",
  globalState2: "",
  globalState3: "",
  globalState4: 0,
  globalState5: false,
}

const productsReducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state
  }
}

export default productsReducer
